kafka:
  application:
    id: kafka-cost-control
  bootstrap:
    servers: localhost:9092
  schema:
    registry:
      url: http://localhost:8081

cc:
  olap:
    enabled: false
    database:
      # technically this is already the default, but let's be explicit
      url: "jdbc:duckdb:/tmp/cc/local.db"
  ai:
    # Natural-language querying of the OLAP data. Requires cc.olap.enabled=true as well,
    # since every question is ultimately answered from the DuckDB table.
    #
    # Model and provider are NOT configured here - see quarkus.langchain4j.* below.
    enabled: false
    # Private mode: the model writes the SQL but never sees the results. Rows go straight to
    # the user. Only the schema shape (metric names, context keys) and the question leave the
    # network; no values and no query output. The cost is that answers are tables, not prose.
    private-mode: false
  admin-password: admin
  metrics:
    aggregations:
      confluent_kafka_server_retained_bytes: max
  topics:
    pricing-rules: pricing-rules
    context-data: context-data
    aggregated: aggregated
    aggregated-table-friendly: aggregated-table-friendly
    raw-data:
      - metrics-raw-telegraf-env

mp:
  messaging:
    connector:
      smallrye-kafka:
        schema:
          registry:
            url: ${kafka.schema.registry.url}

    outgoing:
      pricing-rules-out:
        connector: smallrye-kafka
        topic: ${cc.topics.pricing-rules}
        apicurio:
          registry:
            auto:
              register: true
      context-data-out:
        connector: smallrye-kafka
        topic: ${cc.topics.context-data}
        apicurio:
          registry:
            auto:
              register: true

avro:
  codegen:
    avsc:
      imports: entity-type-enum.avsc

quarkus:
  kafka-streams:
    application-id: ${kafka.application.id}
    topics: ${cc.topics.raw-data},${cc.topics.context-data},${cc.topics.pricing-rules},${cc.topics.aggregated}
    state:
      dir: /tmp/cc/my-streams
    default:
      key:
        serde: org.apache.kafka.common.serialization.Serdes$StringSerde
      value:
        serde: io.confluent.kafka.streams.serdes.avro.SpecificAvroSerde
  security:
    users:
      embedded:
        enabled: true
        plain-text: true
        users:
          admin: ${cc.admin-password}
        roles:
          admin: ADMIN
  http:
    auth:
      basic: true
  smallrye-graphql:
    schema-include-scalars: true
    error-extension-fields: exception,code,classification
    ui:
      enable: true
      root-path: /graphql-ui
      always-include: true
  log:
    category:
      "io.confluent":
        level: WARN
      "org.apache.kafka":
        level: WARN
      "io.spoud.kcc":
        level: INFO
      "org.jooq.Constants":
        level: WARN
  container-image:
    build: true
  # Which LLM the assistant talks to. The provider is decided by the quarkus-langchain4j-*
  # artifact in pom.xml; swap it for -openai, -ollama, -mistral-ai or -gemini and configure
  # the matching block here. OpenRouter works through the OpenAI provider by setting
  # quarkus.langchain4j.openai.base-url=https://openrouter.ai/api/v1
  langchain4j:
    anthropic:
      # Placeholder rather than an empty default: LangChain4j requires the property to be
      # non-empty at startup, and an empty string is converted to null and fails validation.
      # Unused unless cc.ai.enabled=true.
      api-key: ${ANTHROPIC_API_KEY:not-configured}
      chat-model:
        model-name: claude-opus-4-5
        max-tokens: 8000
      # Prompt and response logging is off by default: prompts contain your metric names and
      # context keys, and results can contain topic and application names.
      log-requests: false
      log-responses: false

"%dev":
  mp:
    messaging:
      connector:
        smallrye-kafka:
          apicurio:
            registry:
              url: ${kafka.schema.registry.url}/apis/registry/v2
  cc:
    aggregation-window-size: PT1H
    olap:
      enabled: true
    ai:
      # Still needs ANTHROPIC_API_KEY in the environment to actually answer anything.
      enabled: true
