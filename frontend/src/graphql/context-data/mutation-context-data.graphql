mutation saveContextData($request: ContextDataSaveRequestInput!) {
    saveContextData(request: $request) {
        validFrom
        validUntil
        entityType
        regex
    }
}

mutation deleteContextData($request: ContextDataDeleteRequestInput) {
    deleteContextData(request: $request) {
        id # response is null!
    }
}
