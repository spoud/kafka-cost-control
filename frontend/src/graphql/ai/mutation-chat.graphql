mutation chat($sessionId: String!, $message: String!, $priorTurns: Int) {
    chat(request: { sessionId: $sessionId, message: $message, priorTurns: $priorTurns }) {
        text
        generatedSql
        columns
        rows
        truncated
        contextLost
        complete
        error
    }
}
